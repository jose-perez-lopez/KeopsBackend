machine:
  node:
    version: 0.12.4
  services:
    - redis
test:
  pre:
    - npm start:
        background: true
  override:
    - npm run test-acceptance

deployment:
  staging:
    branch: master
    heroku:
      appname: keops
